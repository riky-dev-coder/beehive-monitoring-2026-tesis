[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# Configuración para mostrar más detalles en fallos
markers =
    asyncio: mark test as async
    integration: mark test as an integration test
    unit: mark test as a unit test
